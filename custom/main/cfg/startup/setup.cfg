// Here replace all binds to your liking, adding '//' in front of the line will comment it out and disable it if you wish to not use that bind.

// demos
bind "" "demosave"
bind "" "demoreset"

// viewmodel fov
bind "" "upfov"
bind "" "downfov"
bind "" "toggleminviewmodels"
bind "" "toggleviewmodel"

// netgraph
bind "" "toggle_netgraph"

// fakelag
bind "" "fakelagcycle"
bind "" "resetfakelag"
bind "" "toggle_lag"
bind "" "+lag_on"
bind "" "+peek_desync"

// movement
bind "" "+ljhold"
bind "" "+mjhold"
bind "" "+align" 
bind "" "ctap"

// menu
bind "`" "cheatmenu"

// startup aliases
gfx // can change with mcoms presets (this is already set to a low gfx preset)
movementbinds // this includes the movement config binds, as well as a null movement script